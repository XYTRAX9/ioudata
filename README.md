# Система подбора сотрудников для клиентов

## Описание
Система предназначена для эффективного подбора сотрудников для работы с клиентами на основе их стрессоустойчивости, истории коммуникаций и предпочтений клиентов. Включает в себя API на FastAPI и простой чат для взаимодействия.

## Основные возможности

### API (Бэкенд)
- **Аутентификация**: JWT для сотрудников и клиентов.
- **Управление сотрудниками**: CRUD операции, управление группами и статусами.
- **Управление клиентами**: CRUD операции, управление предпочтениями и черными списками.
- **Управление коммуникациями**: Запись истории взаимодействий, оценка успешности.
- **Управление тестами**: Создание и прохождение тестов (множественный выбор), автоматическое обновление группы сотрудника на основе результатов.
- **Подбор сотрудников**: Автоматический подбор сотрудника для клиента на основе истории, предпочтений и нагрузки.

### Чат (Простой фронтенд)
- **Вход**: Отдельные страницы входа для клиентов и операторов.
- **Обмен сообщениями**: Real-time обмен сообщениями через WebSocket.
- **Назначение оператора**: Автоматическое назначение доступного оператора новому клиенту.
- **Уведомления**: Операторы получают уведомления о новых клиентах.

## Установка и запуск

### Требования
- Python 3.10+
- FastAPI
- SQLAlchemy
- passlib[bcrypt]
- python-jose[cryptography]
- uvicorn[standard]
- aiohttp (для чата)
- python-multipart (для форм)

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### База данных
- Система использует SQLite по умолчанию (`matching_system.db`). Файл создается автоматически.
- Для сброса базы данных просто удалите файл `matching_system.db` и перезапустите приложение.

### Запуск приложения
```bash
uvicorn app.main:app --reload
```
Приложение будет доступно по адресу `http://localhost:8000`.

## Первый запуск

При первом запуске автоматически создается тестовый оператор:
- **Логин**: `test_operator`
- **Пароль**: `test123`

Вы можете использовать эти данные для входа в панель оператора чата.

## Использование API

Документация API доступна по адресу `http://localhost:8000/docs` после запуска приложения.

### Аутентификация
- Используйте эндпоинт `/auth/token` для получения JWT токена сотрудника (передавая `username` и `password` в form data).
- Используйте эндпоинт `/auth/client/register` для регистрации клиента.
- Используйте эндпоинт `/chat/client/login` (POST) для входа клиента в чат и получения `client_id`.
- Используйте эндпоинт `/chat/support/login` (POST) для входа оператора в чат и получения `operator_id`.
- Для защищенных эндпоинтов передавайте токен в заголовке `Authorization: Bearer your-token-here`.

### Основные эндпоинты
(См. документацию `/docs` для полного списка и деталей)

- `/auth/`: Эндпоинты для аутентификации и регистрации.
- `/employees/`: Управление сотрудниками.
- `/clients/`: Управление клиентами.
- `/communications/`: Управление коммуникациями.
- `/tests/`: Управление тестами и результатами.
- `/profile/`: Получение профиля сотрудника.
- `/chat/`: HTML страницы чата и эндпоинты входа.
- `/chat/ws/`: WebSocket эндпоинты для чата.

## Чат

- **Оператор поддержки**: `http://localhost:8000/chat/support` (Логин: `test_operator`, Пароль: `test123`)
- **Клиент**: `http://localhost:8000/chat`
  - Сначала зарегистрируйте клиента через API (`/auth/client/register`) или используйте тестового клиента, если вы его создали.
  - Используйте email и пароль клиента для входа.

## Заметка для фронтендера

Привет! 👋

Этот README описывает бэкенд и простой чат на HTML/JS.

- **API**: Основа системы - это FastAPI бэкенд. Все основные операции (управление пользователями, тестами, коммуникациями, подбор) реализованы через API. Документация доступна в `/docs`.
- **Чат**: В `templates/` лежат простые HTML шаблоны (`chat.html`, `support.html`) с JavaScript для базового чата через WebSocket. Они используют эндпоинты `/chat/client/login`, `/chat/support/login` для входа и `/chat/ws/...` для WebSocket соединения.
- **Аутентификация**: Для API используется JWT (получается через `/auth/token`). Для WebSocket чата используется простая система сессий на основе словарей в памяти (`client_tokens`, `operator_tokens` в `chat.py`), которые заполняются после успешного логина через `/chat/.../login`.
- **Ваши задачи**: Вы можете:
    1.  Разработать полноценный фронтенд (например, на React, Vue, Angular), который будет взаимодействовать с API бэкенда.
    2.  Улучшить существующие HTML/JS шаблоны чата, добавив больше функций (отображение списка клиентов для оператора, история чата, статусы и т.д.).
    3.  Интегрировать функционал чата в основной фронтенд.
- **Ключевые эндпоинты для старта**: `/auth/token`, `/auth/client/register`, `/employees/me`, `/clients/me`, `/chat/.../login`, `/chat/ws/...`

### Необходимые страницы для фронтенда:

**Общие:**
1.  **Страница входа**: Форма для входа сотрудников (операторов/администраторов) и клиентов. Должна использовать `/auth/token` или `/chat/.../login`.
2.  **Страница регистрации клиента**: Форма для регистрации новых клиентов (`/auth/client/register`).

**Для Сотрудника (Оператора/Администратора):**
3.  **Личный кабинет / Профиль сотрудника**: Отображение информации о текущем сотруднике (`/employees/me`), его группе, уровне стресса, истории коммуникаций и результатах тестов (`/profile/`).
4.  **Список тестов**: Отображение доступных тестов (`/tests/multiple-choice/`).
5.  **Страница прохождения теста**: Интерфейс для ответов на вопросы теста (`/tests/multiple-choice/{test_id}/submit`).
6.  **Чат оператора**: Интерфейс чата для оператора. Должен включать:
    *   Список подключенных клиентов (получение информации через WebSocket).
    *   Окно диалога с выбранным клиентом.
    *   Поле для ввода сообщения.
    *   Использовать `/chat/support/login` для входа и `ws://.../chat/ws/support/{operator_id}` для WebSocket.

**Для Администратора (Суперпользователя):**
7.  **Управление сотрудниками**: 
    *   Отображение списка всех сотрудников (`/employees/`).
    *   Возможность создания новых сотрудников (`/employees/`).
    *   Возможность изменения группы сотрудника (`/employees/{employee_id}/group`).
    *   (Опционально) Просмотр детальной информации по сотруднику.
8.  **Управление тестами**: 
    *   Возможность создания новых тестов с вариантами ответов (`/tests/multiple-choice/`).
    *   (Опционально) Просмотр и управление существующими тестами.

**Для Клиента:**
9.  **Профиль клиента**: Отображение информации о текущем клиенте (`/clients/me`), истории коммуникаций.
10. **Чат клиента**: Интерфейс чата для клиента.
    *   Окно диалога с оператором.
    *   Поле для ввода сообщения.
    *   Использовать `/chat/client/login` для входа и `ws://.../chat/ws/client/{client_id}` для WebSocket.
    *   (Опционально) Возможность указать предпочтительных/нежелательных сотрудников.
    *   (Опционально) Возможность оценить диалог.

Удачи в разработке!

## Возможные сценарии использования

1.  **Клиент обращается в поддержку**: Клиент входит в чат, система автоматически подбирает и назначает ему доступного оператора. Начинается диалог.
2.  **Администратор создает тест**: Администратор (суперпользователь) создает новый тест с вариантами ответов через API.
3.  **Сотрудник проходит тест**: Сотрудник входит в систему (через будущий фронтенд или API), видит доступные тесты, проходит их. Его группа (`normal`, `slightly_below`, `significantly_below`) автоматически обновляется на основе среднего балла.
4.  **Подбор сотрудника для звонка**: Система (через будущий фронтенд или API) использует эндпоинт `/clients/{client_id}/match` для подбора наиболее подходящего сотрудника для исходящего звонка клиенту, учитывая историю коммуникаций и черный список клиента.
5.  **Оператор общается с несколькими клиентами**: Оператор в панели поддержки видит список подключенных клиентов и может переключаться между диалогами (требует доработки фронтенда чата).
6.  **Просмотр профиля сотрудника**: Администратор или сам сотрудник просматривает профиль, включая историю коммуникаций и результаты тестов.

## Безопасность

- Пароли хешируются с использованием bcrypt.
- API защищено JWT токенами.
- Чат использует простую систему токенов/сессий (для продакшена может потребоваться усиление).

## Логирование

- Базовое логирование ведется в консоль.
- Логи чата выводятся с помощью стандартного модуля `logging`.

## Обработка ошибок

- API возвращает стандартные HTTP ошибки.
- WebSocket закрывается с кодом `4001` при ошибках авторизации или ненайденных пользователях.
